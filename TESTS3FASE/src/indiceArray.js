export function indiceArray(num, numB){
    const valor = [4, 12, 2, 5, 78, 62,]

    if(num => 0 && num < valor.length){
        numB = valor[num]
        return numB
    }
    else{
        return "Erro"
    }
   
}